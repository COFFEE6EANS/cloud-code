server:
  port: 7777
  servlet:
    context-path: /uaa
spring:
  application:
    name: auth-server
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:1000/eureka/

